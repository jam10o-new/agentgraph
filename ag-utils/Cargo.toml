[package]
name = "ag-utils"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["fs", "rt-multi-thread", "io-util"] }
chrono = { version = "0.4.44", features = ["serde"] }
